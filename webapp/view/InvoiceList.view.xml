<mvc:View
    controllerName="sapui5.controller.InvoiceList"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">

    <List 
        id="invoiceList"
        headerText="{i18n>invoiceListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{
            path:'invoice>/Invoices',
            sorter: {
                path: 'ShipperName',
                group: true
            }
        }" >
        <headerToolbar>
            <Toolbar id="SearchComponent">
                <Title 
                    id="toolbarTitle"
                    text="{i18n>invoiceListTitle}"/>
                <ToolbarSpacer 
                    id="spacer"/>
                <SearchField 
                    id="search"
                    width="50%"
                    search=".onFilterInvoices"/>
            </Toolbar>
        </headerToolbar>

        <items>
            <ObjectListItem 
                id="obj_list"
                title="{invoice>Quantity} x {invoice>ProductName}"
                number="{
                        parts: [
                            {path:'invoice>ExtendedPrice'},
                            {path:'view>/currency'}],
                            formatter: '.formatCurrency'
                        }"
                numberUnit="{view>/currency}"
                numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success'}">
                <firstStatus>
                    <ObjectStatus id="Status_text" text="{
                        path: 'invoice>Status',
                        formatter: '.statusText'
                    }"/>
                </firstStatus>
            </ObjectListItem>
        </items>

    </List>
        

</mvc:View>